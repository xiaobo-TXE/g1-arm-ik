"""g1_arm_bringup -- integration layer: bridges, TF, config and launch files.

This package is the only one that knows about the *robot*, as opposed to the
arm's kinematics. It contains no control logic of its own.
"""

__version__ = "0.1.0"
